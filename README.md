A fanmade chart editor for DanceRail3 with the goal of full compatability with the official game.

## Build and host locally

Run:

```bash
./scripts/build-and-host.sh
```

Optional environment variables:

- `PORT` (default: `4173`)
- `HOST` (default: `0.0.0.0`)
